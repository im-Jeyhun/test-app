@using EduMapBackendProject.DAL.Entities
@model List<FeedBack>

<div class="col-lg-12">
    <div class="row justify-content-end">
        <div class="">
            <a asp-controller="feedback" asp-action="create" class="btn btn-secondary">Add new FeedBack</a>
        </div>
    </div>
</div>
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">FeedBack</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Tittle</th>
                            <th>Desctiption</th>
                            <th>Image</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var feedBack in Model)
                        {
                            <tr>
                                <td>@feedBack.FullName</td>
                                <td>@feedBack.Position</td>
                                <td>@feedBack.Content</td>
                                <td> <img src="~/img/@feedBack.ImagePath" width="10%"> </td>
                                <td class="text-end">
                                    <a asp-controller="feedback" asp-action="update" asp-route-id="@feedBack.Id"><i class="las la-pen text-secondary font-16"></i></a>
                                    <form method="POST"asp-controller="feedback" asp-action="delete" asp-route-id="@feedBack.Id" id="feedback-form-@feedBack.Id"></form>
                                    <button type="submit" form="feedback-form-@feedBack.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>